# MirvkBuntu Build Profiles

- `slim.profile` — graphical live/RAM-oriented build.
- `minimum.profile` — minimal bootable MirvkBuntu build.
- `full.profile` — complete desktop build; implemented by `build-desktop.sh`.

The profiles describe selection policy. They do not replace the authoritative source tree or native build scripts.
