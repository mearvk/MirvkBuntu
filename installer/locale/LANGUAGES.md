# Installer Locale Data

The installer uses locale identifiers rather than embedding translated OS components. The initial catalog covers common English, European, East Asian, and Brazilian Portuguese locales. Additional locales can be added without changing the installer engine.

Keyboard layouts are selected independently from language. Time zones use IANA identifiers.